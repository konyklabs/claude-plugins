---
type: regex
target: trace
pattern: "TC-auth-"
---
`auth.sign-in.valid-password` is an `auth` tile, so a case naming it gets an id of the form `TC-auth-<nnn>` (`plugins/signoff/formats.md`, "Identifiers"), not some other area's prefix.
